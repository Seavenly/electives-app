const assignElectives = require('./assignElectives');
const resetElectives = require('./resetElectives');
const setElectivesCap = require('./setElectivesCap');
const setStudentLists = require('./setStudentLists');
const randomizeStudentLists = require('./randomizeStudentLists');
const logElectives = require('./logElectives');
const fromCsv = require('./fromCsv');
const createHtmlLog = require('./createHtmlLog');

module.exports = {
  assignElectives,
  resetElectives,
  setElectivesCap,
  setStudentLists,
  randomizeStudentLists,
  logElectives,
  fromCsv,
  createHtmlLog,
};
